## Goal

Define `core/workflows/longform-writing.md` with this shape:

```text
intent
  -> context-pack
  -> angle
  -> structure
  -> draft
  -> review
  -> rewrite
  -> final
```

The workflow should be adaptable by role-cards without becoming a runtime.
