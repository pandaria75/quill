## Goal

- Remove old CLI runtime.
- Delete model client / step / run / status / new command behavior.
- Delete old smoke tests.
- Preserve npm package name `@pandaria/quill`.
- Add `docs/archive/cli-mvp-postmortem.md`.
- Update README and package metadata away from workflow CLI positioning.

## Non-Goals

- Do not rebuild a standalone runtime.
- Do not add model provider clients.
