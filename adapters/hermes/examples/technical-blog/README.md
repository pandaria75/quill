## Hermes technical-blog smoke example

Use `workspace/` as a bounded seed workspace for the commands documented in `../../README.md`.

- No secrets are required.
- Copy it to `/tmp/opencode/hermes-smoke-s4e` before smoke so generated artifacts stay outside the repository.
- The adapter writes artifacts into the copied workspace on demand.
