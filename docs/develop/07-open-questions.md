# Open Questions

## MVP Questions

1. Should `brief` and `sources` be model-generated in the first MVP, or only template-created and human-edited?
2. Should `quill run` stop at human review points by default, or only print reminders?
3. Should the first MVP include `--force` for `run`, or only for `step`?
4. Should artifact frontmatter status be updated automatically in the MVP?

## Quality Questions

1. What style profile fields are most useful beyond plain Markdown prose?
2. Should review checklists be workflow-specific or style-specific?
3. How should Quill detect overly generic AI language without becoming brittle?

## Future Questions

1. When is Markdown + block markers enough?
2. What real need would justify Article IR?
3. Which Quill primitives are stable enough to feed back into Marionettist?
